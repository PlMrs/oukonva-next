import { HeaderContent } from "@/components/header/Content"
import { FC } from "react"

export const Header: FC = () => {

    return (
        <header>
            <HeaderContent />
        </header>
    )
}